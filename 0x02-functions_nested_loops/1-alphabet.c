#include "main.h"
/**
*main - print alphabets in lower case
*return: 0
**/
void print_alphabet(void);
{
	for (letter <= 'a'; letter <= 'z'; letter++)
	_putchar(letter);
	_putchar('\n');
	return (0);
}
